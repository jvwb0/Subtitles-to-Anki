# PyTorch with CUDA 12.8 (RTX 4070 / Ada Lovelace, driver 596+)
# Install with: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.10.0+cu128
torchaudio==2.10.0+cu128
torchvision==0.25.0+cu128

# Core transcription
faster-whisper==1.2.1
ctranslate2==4.6.3
tokenizers==0.22.2

# Audio capture (Windows WASAPI loopback)
PyAudioWPatch==0.2.12.8

# VAD model download
huggingface_hub==1.3.2

# Numerics
numpy==2.4.1

# HTTP (definition/translation API calls)
requests
httpx==0.28.1
certifi==2026.1.4

# Translation (dual subtitle feature)
deep-translator==1.11.4

# Optional: RAM monitor in GUI
psutil==7.2.2

# Misc
tqdm==4.67.1
